## Using Docker

The application keeps the `hyperglass` service and configuration names for compatibility. Pull the Ultraglass repository, then update the checked-out release before rebuilding the containers.

```shell copy
cd /opt/hyperglass
docker compose down
docker compose rm -f
git fetch --tags
git checkout v3.2.2
docker compose build
docker compose up
```
